# Distributed-Model-Training-using-Ray-
This project implements a distributed machine learning pipeline for predicting Azure Virtual Machine (VM) failures using Ray. The solution is designed to efficiently train machine learning models on large-scale cloud telemetry data by distributing computations across multiple workers.
